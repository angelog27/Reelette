{
  "name": "reelette-functions",
  "version": "1.0.0",
  "engines": { "node": "18" },
  "main": "lib/index.js",
  "scripts": {
    "build":          "tsc",
    "build:watch":    "tsc --watch",
    "deploy":         "npm run build && firebase deploy --only functions",
    "import-exports": "node scripts/importExports.js",
    "seed-now":       "npm run build && node scripts/runSeed.js"
  },
  "dependencies": {
    "axios":              "^1.7.0",
    "firebase-admin":     "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "@types/node":  "^18.0.0",
    "typescript":   "^5.4.0"
  }
}
